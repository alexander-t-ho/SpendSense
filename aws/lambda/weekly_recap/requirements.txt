# Lambda function dependencies for weekly recap
# These will be packaged with the Lambda function

# Core dependencies (minimal for Lambda)
sqlalchemy==2.0.23
python-dateutil==2.8.2

# AWS SDK (already available in Lambda runtime)
# boto3 is pre-installed in Lambda Python runtime

